"""HTTP/JSON service layer — the machine-callable face of the engine.

The 2026 automatic track is a competition BETWEEN virtual assistants; whatever
protocol the organisers publish, it will need a headless, machine-callable
interface to the retrieval core. This package provides it (FastAPI), and the
interactive UI / auto-agent can share the exact same endpoints.
"""
